# ProPresentation
This windows application allows you to interpret video as a powerpoint presentation.
This works by setting keyframes in video and this keyframes will become slides and anything between keyframes becomes transition.
